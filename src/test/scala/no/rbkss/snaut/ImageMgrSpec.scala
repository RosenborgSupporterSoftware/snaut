package no.rbkss.snaut

import org.scalatest._

class ImageMgrSpec extends FlatSpec with Matchers {
    "ImageMgr" should "construct image for forecast" is pending
    it should "set three forecasts together" is pending
    it should "generate png from svg files" is pending
}
